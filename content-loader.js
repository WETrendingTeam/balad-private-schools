/* BALAD public-page compatibility loader.
 * The public pages keep their bundled assets and text.
 * Live content editing is not part of the current release, so this file
 * intentionally does not call a Netlify backend.
 */
document.addEventListener("DOMContentLoaded", () => {});
